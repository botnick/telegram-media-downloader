{
  "name": "@tgdl/web",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Static SPA assets — served by @tgdl/server in production. React + Vite migration is staged for a follow-up PR; for now the original vanilla-DOM modules under public/js are still the source of truth.",
  "scripts": {
    "build": "echo 'static assets — no build step yet'",
    "lint": "echo 'lint placeholder'",
    "typecheck": "echo 'typecheck placeholder'",
    "test": "echo 'test placeholder'",
    "clean": "rm -rf dist .vite *.tsbuildinfo"
  }
}
