{
  // Root tsconfig — only references each workspace's project. IDEs use
  // this to resolve types across the monorepo. Per-workspace builds run
  // through their own tsconfig + Turbo's pipeline.
  "files": [],
  "references": [
    { "path": "./packages/shared" },
    { "path": "./apps/server" },
    { "path": "./apps/cli" }
  ]
}
